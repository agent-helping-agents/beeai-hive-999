{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "gunicorn --chdir codex-superlab-recreation web_dashboard:app --bind 0.0.0.0:$PORT --workers 2",
    "healthcheckPath": "/"
  }
}